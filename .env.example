# CyberSentinel DLP - Environment Configuration
# Copy this file to .env and configure with your values

# ============================================
# SECURITY SETTINGS (CHANGE THESE!)
# ============================================
SECRET_KEY=change-this-to-a-random-secret-key-min-32-chars
POSTGRES_PASSWORD=change-this-strong-postgres-password
MONGODB_PASSWORD=change-this-strong-mongodb-password
REDIS_PASSWORD=change-this-strong-redis-password

# ============================================
# NETWORK CONFIGURATION
# ============================================
# Your Ubuntu server's IP address
HOST_IP=your-ubuntu-server-ip
VITE_API_URL=http://SERVER-IP:55000/api/v1
VITE_WS_URL=ws://SERVER-IP:55000/ws

# ============================================
# DATABASE SETTINGS
# ============================================
# PostgreSQL Configuration
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=dlp_user
POSTGRES_DB=cybersentinel_dlp

# MongoDB Configuration
MONGODB_HOST=mongodb
MONGODB_PORT=27017
MONGODB_USER=dlp_user
MONGODB_DB=cybersentinel_dlp

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379

# OpenSearch Configuration (Event Storage & Search)
OPENSEARCH_HOST=opensearch
OPENSEARCH_PORT=9200
OPENSEARCH_USER=admin
OPENSEARCH_PASSWORD=CyberSentinel2025!
OPENSEARCH_USE_SSL=true
OPENSEARCH_VERIFY_CERTS=false
OPENSEARCH_INDEX_PREFIX=cybersentinel

# ============================================
# APPLICATION SETTINGS
# ============================================
ENVIRONMENT=production
DEBUG=False
PORT=55000
APP_NAME=CyberSentinel DLP
APP_VERSION=2.0.0

# ============================================
# CORS SETTINGS (Add your dashboard URLs)
# ============================================
# Supported formats for CORS_ORIGINS / ALLOWED_HOSTS:
# - JSON list (recommended): ["http://localhost:3000","http://192.168.1.63:3000"]
# - Comma-separated string:  http://localhost:3000,http://192.168.1.63:3000
CORS_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000","http://SERVER-IP:3000"]

# Trusted hosts (used when DEBUG=False). Include your server IP/hostname here.
ALLOWED_HOSTS=["localhost","127.0.0.1","SERVER-IP"]

# ============================================
# EMAIL ALERTS (OPTIONAL)
# ============================================
SMTP_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@company.com
SMTP_PASSWORD=your-email-password
SMTP_FROM=dlp-alerts@company.com
ALERT_EMAIL=admin@company.com

# ============================================
# WAZUH SIEM INTEGRATION (OPTIONAL)
# ============================================
WAZUH_ENABLED=false
WAZUH_HOST=your-wazuh-server
WAZUH_PORT=55000
WAZUH_USER=wazuh-api-user
WAZUH_PASSWORD=wazuh-api-password

# ============================================
# LOGGING SETTINGS
# ============================================
LOG_LEVEL=INFO
LOG_FILE_PATH=/var/log/cybersentinel
MAX_LOG_SIZE_MB=100
LOG_RETENTION_DAYS=30

# ============================================
# QUARANTINE SETTINGS
# ============================================
QUARANTINE_ENABLED=true
QUARANTINE_PATH=/var/quarantine/dlp
QUARANTINE_MAX_SIZE_GB=50

# ============================================
# PERFORMANCE TUNING
# ============================================
MAX_WORKERS=4
MAX_CONNECTIONS=100
REQUEST_TIMEOUT=30

# ============================================
# COMPLIANCE SETTINGS
# ============================================
COMPLIANCE_MODE=GDPR,HIPAA,PCI-DSS
DATA_RETENTION_DAYS=90
AUDIT_LOG_ENABLED=true
