# Constraints file to prevent pip from auto-upgrading key packages.

# Pin numpy (scikit-learn / pandas build against it)
numpy==1.26.2

# Pin data science packages
pandas==2.1.4
scikit-learn==1.3.2

# Keep protobuf in a safe range (transitive dep of grpc / google libs)
protobuf>=3.20.3,<5.0.0

# NOTE: tensorflow / torch / transformers / spacy / keras / h5py pins were removed
# along with those packages — they were imported nowhere and bloated the image
# (~5GB unpacked). Re-add deliberately if a deep-learning model is ever introduced.
