# FastAPI and Server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.23
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.0
# pymongo 4.6.0 has CVE-2024-5629 (BSON OOB read) — fixed in 4.6.3
pymongo==4.6.3
motor==3.3.2
redis==5.0.1
opensearch-py==2.4.2
elasticsearch==8.11.1

# Authentication & Security
# python-jose 3.3.0 has CVE-2024-33664 (JWE PBES2 DoS, CRITICAL) and
# CVE-2024-33663 (algorithm confusion with OpenSSH ECDSA keys, HIGH).
# Both fixed in 3.4.0.
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.1
# cryptography 41.0.7 has CVE-2023-50782 (Bleichenbacher timing oracle)
# and CVE-2024-26130 (NULL deref in PKCS#12). Fixed in 42.0.4+.
cryptography==42.0.8

# HTTP & API
httpx==0.25.2
# aiohttp 3.9.1 has CVE-2024-23334 (path traversal), CVE-2024-30251
# (DoS via multipart), and CVE-2024-27306 (XSS in static index).
# All fixed in 3.9.5+.
aiohttp==3.9.5
aiofiles==23.2.1
# requests 2.31.0 has CVE-2024-35195 (verify=False session persistence).
requests==2.32.3
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.154.0
msal==1.24.0

# Monitoring & Logging
prometheus-client==0.19.0
python-json-logger==2.0.7
structlog==23.2.0

# Validation & Parsing
email-validator==2.1.0
python-dateutil==2.8.2
bleach==6.1.0
reportlab==4.0.7

# ML & Data Processing
# The sensitivity classifier uses scikit-learn only (TF-IDF + LogisticRegression);
# numpy/pandas are its data deps. tensorflow / torch / transformers / spacy / nltk
# were pinned here but imported NOWHERE in the codebase — removed (~5GB unpacked)
# to slim the manager image. If a real deep-learning model is ever added, pull the
# needed framework back in deliberately.
numpy==1.26.2
pandas==2.1.4
scikit-learn==1.3.2

# Text Processing
regex==2023.10.3

# Utilities
python-dotenv==1.0.0
tenacity==8.2.3
schedule==1.2.0
celery==5.3.4
kombu==5.3.4

# Runtime system metrics
psutil==5.9.6
# Dev/test tools (pytest, pytest-asyncio, pytest-cov, Faker, black, flake8, mypy,
# pre-commit) live in requirements-dev.txt — kept OUT of the production image.

# MFA (TOTP + QR)
pyotp==2.9.0
qrcode==7.4.2

# Threat intel — STIX 2.1 / TAXII 2.1 IOC exchange
stix2==3.0.1
taxii2-client==2.3.0

# Document text extraction — lets the classifier read binary documents
# (pdf/docx/xlsx/pptx). Without these, a sensitive PDF decodes to compressed
# bytes, classifies as "Public", and is allowed through on every channel.
pypdf==4.3.1
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==0.6.23
py7zr==0.21.1          # .7z archives (zip/tar/gz are stdlib)

# OCR for scanned PDFs / images (no text layer). Needs the tesseract-ocr and
# poppler-utils system packages (see Dockerfile). Optional at runtime: if absent,
# extraction reports the file "unreadable" (blocked by policy), never "clean".
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==10.4.0
