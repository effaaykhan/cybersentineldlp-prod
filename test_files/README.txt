This folder contains sample files for Google Drive local monitoring tests.

Steps:
1. Copy the files from this folder into your Windows Google Drive path (e.g., `G:\My Drive\Projects\test_files`).
2. Let the Windows agent run; it will detect create/modify events and send them to the manager.

Files:
- `PII_sample.txt` – contains mock sensitive data (SSN, PAN) for verification.

